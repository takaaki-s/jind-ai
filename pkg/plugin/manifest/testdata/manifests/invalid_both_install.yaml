schema_version: 1
name: hello-plugin
version: 0.1.0
description: Both install variants set
jin: ">=0.7.0"
install:
  source:
    build:
      - go build
    entrypoint: ./bin/hello
  release_asset:
    pattern: "hello-{os}-{arch}"
